# 跨评估维度比较：独立结果审计

运行目录：`evaluation/final_20260922T021353930250Z`。

当前状态：完整运行及只读复核均已完成，未发现阻断。本审阅者只读取现有 JSON、文件长度与冻结归档，并作有理数比较和汇总算术，没有运行数学求解、完整验证器或性能测试，没有读取任何种子，也没有修改源码、协议、题单或历史冻结文件。

## 常势解析控制题

预声明答案来自 `notes/ANALYTIC_CONTROLS.md`。单位 S² 上常势为 q 时，全空间最低谱为 q；平均零空间最低谱为 q+2。因此 S14/S15/S16 分别为 `-6`、`1/1000000`、`-1/1000000`。

逐条读取四系统、三题、五重复共 60 份 `result.json` 及已有的 45 份 `output.json`，核对系统、题号、重复编号，并核对两份记录的 response、assessment、verifier_result 相等。B 的 15 次记录在准备阶段即不支持，未生成求解输出，其 `result.json` 仍完整保留失败分母；没有将缺失的求解输出计为成功。

以下“有效”指本轮原始记录中的外部完整验证通过；本补充审计另以 `Fraction` 精确核查每份有效证书 `lower ≤ 解析值 ≤ upper` 及 `upper-lower ≤ 1/1000000`。结果：40 份有效最低谱证书全部通过，0 个错误包围或超宽区间；20 次不支持均未伪装为最低谱成功。

| 系统 | S14：−6 | S15：+10⁻⁶ | S16：−10⁻⁶ |
|---|---|---|---|
| baseline | 5/5，有效精确点区间 | 5/5，有效精确点区间 | 5/5，有效精确点区间 |
| A | 5/5，有效包围区间 | 0/5；五次均不支持 | 5/5，有效包围区间 |
| B | 0/5；五次均准备不支持 | 0/5；五次均准备不支持 | 0/5；五次均准备不支持 |
| C | 5/5，有效精确点区间 | 5/5，有效精确点区间 | 5/5，有效精确点区间 |

A 的五次重复在每题均返回同一组精确端点：

- S14：`[-432345564227567641/72057594037927936, -1610612731/268435456]`，宽度 `1342177305/72057594037927936`，包含 −6。
- S16：`[-18052302401284349/18014398509481984000000, -67000067/67108864000000]`，宽度 `67108864108797/18014398509481984000000`，包含 −10⁻⁶。
- S15：五次外层最低谱证书均为 `null`、成功均为 `false`、状态均为 `unsupported`，原因均为 `no_certified_strictly_negative_eigenvalue`；保留的来源响应负谱计数均为 0。没有将无负谱错误转换为最低谱等于零。

此检查验证了这三道解析控制题上的返回行为；不等于已对其余非恒定势作独立数学证明，也不等于攻击所有证书分支。

## 冻结身份

已只读核对本轮 `evaluation/LATEST_FREEZE.json` 指向的清单字节哈希，与指针一致。清单中的 13 项新目录文件逐一重算 SHA-256 均一致，包括全部新 Python 文件、题单、参数族、协议、环境记录及预声明解析控制说明。核心适配与评估代码为：

| 文件 | 当前值与冻结值一致的 SHA-256 |
|---|---|
| `a_adapter.py` | `e48ae19acaa5c54ec0b88d0366b2e0498c7d9d374e68c4d846366472f9f705e8` |
| `b_adapter.py` | `1556e80074b94395b5e7dc8344d36ea8880da1ec089ddb2150e3f2ebe09935b5` |
| `engines.py` | `40074e6f400149efa74d0bd41d6bc22868a930bd540d0f831fd31e3d1f5b7435` |
| `benchmark.py` | `c395ad5a0e84e433a066a5e2dff9d60fb0445958fc21c07a7deffa99b22576d6` |

本次未扩展读取其他轮的工作目录源码或留出数据。完整报告的 `identity_before` 与 `identity_after` 完全相等，且等于本轮冻结清单身份。另读取本轮自己的源码归档：604 项索引与冻结清单文件集合一致，604 项归档原文字节的 SHA-256 全部匹配；13 项新目录文件的当前哈希再次全部匹配。对历史工作目录原 567 文件的当前状态，本审计依据运行中保存的前后保全结果；没有另行遍历历史工作目录。

## 完整运行的固定分母与原始记录

独立检查确认为 18×4×5＝360 次冷单题、2×4×5＝40 个批量会话、40×16＝640 个查询及 640 次实际执行的独立冷重放，没有以未执行占位行充当冷重放。题号、系统、重复编号的集合等于预定笛卡尔积；每个查询与对应冷重放的原题及其哈希一致。

读取 360 份单题、640 份冷重放的原始 `result.json`，其成功、系统、模式、重复、原题哈希、进程费用和认证判定均与报告一致；40 份会话原始进程记录也一致。对每个会话的第 1、4、16 题再读取原始查询输出，共 120 份，其判定及输出/证书字节数一致。未再次执行或逐项重证重复出现的参数银行数学内容。

按目标分别复算五次稳定成功数：

| 系统 | 最低谱稳定成功 / 16 | 谱成功次数 / 80 | L² 逼近稳定成功 / 2 | 逼近成功次数 / 10 |
|---|---:|---:|---:|---:|
| baseline | 15 | 75 | 2 | 10 |
| A | 3 | 15 | 0 | 0 |
| B | 8 | 40 | 0 | 0 |
| C | 15 | 75 | 2 | 10 |

四系统五次均成功的共同单题只有 **S01**。A、B、C 与基线的配对成功子集分别为 3、8、17 题；这些不同子集不能用来对三算法作一个全体速度排名。两参数族的四系统均为 80/80 查询及冷重放成功，总计 640/640，因此批量的相同查询成本可直接比较。原有压力题和所有失败仍在固定分母中。

## 费用、内存与序列化体积

独立复算四系统全部单题尝试的 wall/CPU 总和、每题五次的 wall/CPU/RSS 中位数、全部 120 个批量前缀的准备加查询再加冷重放费用，以及 40 个完整会话退出后的总费用；均与报告相符。报告的 `valid_frozen_comparison` 和 `complete_fixed_denominators` 均为真。

共同单题 S01 的五次中位数如下。wall/CPU 为秒，RSS 与 JSON 体积为字节；这是该题完整既有路线加适配的成本，不是原生内核的等功能竞速。

| 系统 | wall | CPU | 峰值 RSS | 输出 JSON | 证书 JSON |
|---|---:|---:|---:|---:|---:|
| baseline | 0.352427 | 0.349272 | 37,486,592 | 31,421 | 23,076 |
| A | 0.752273 | 0.747293 | 38,043,648 | 34,465 | 15,961 |
| B | 0.794589 | 0.791518 | 38,060,032 | 60,231 | 59,169 |
| C | 0.220649 | 0.217558 | 37,568,512 | 23,835 | 20,166 |

下表是每个 16 查询会话的**退出后完整进程费用加全部 16 次冷重放**的五次中位数，不是只截到最后查询结束的前缀。

| 参数族 | 系统 | wall 秒 | CPU 秒 |
|---|---|---:|---:|
| F1 窄区间 | baseline | 6.084440 | 6.025848 |
| F1 窄区间 | A | 13.219535 | 13.167340 |
| F1 窄区间 | B | 3.357516 | 3.312550 |
| F1 窄区间 | C | 3.937952 | 3.890362 |
| F2 宽区间 | baseline | 6.106267 | 6.047973 |
| F2 宽区间 | A | 12.430397 | 12.376813 |
| F2 宽区间 | B | 4.040705 | 3.993195 |
| F2 宽区间 | C | 3.898427 | 3.853118 |

检查了全部 1,040 个独立进程的 CPU＝user+system；Darwin 的 `ru_maxrss` 原值与报告的 RSS 字节一致。串行过程的整体内存字段取各进程峰值的最大值，没有将峰值相加或作差。前缀使用会话累计 CPU/RSS，不能把会话 RSS 称为某一道查询的独立峰值。

对所有已记录非零载荷的单题/冷重放，输出和证书体积分别与实际文件长度核对，共 950 个输出、910 个证书文件；另外抽查 120 个查询的两类载荷文件。40 个准备响应的规范 JSON 字节数亦独立重算一致。例如 B 的 F1/F2 准备响应中位数分别为 77,623 / 116,525 字节，完整银行载荷为 57,487 / 87,766 字节；查询证书中位数为 59,178 / 89,421 字节。后者包含自持银行内容，不能只报短查询参数作为完整证书体积。JSON 字节不等于模型 token，序列化银行体积也不等于运行时堆内存。

本次共完成 15,221 个记录字段、算术或哈希比较，无不一致；该数量是审计比较次数，不是新增数学测试题数。

## 审计绑定与边界

- 最终报告 SHA-256：`35e8e49828e7f88262ba482013043af1997aec06a0fd2116e0160a6245fc14cc`。
- 本轮源码归档 SHA-256：`f27d937c2936a42b94cf40fbdcc09abedddd00e4bec2902eecc056ec26c32f2a`。
- 未发现冻结身份、固定分母、认证成功计数或成本汇总的阻断。五次共享机器上的公开开发题结果不能自动外推为未见任务泛化、任意空间维数支持或统计显著的普遍提速。
