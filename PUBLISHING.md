# 2026-09-21 公开发行说明

2026-09-22 的 C3 / C3.1 增量发行及公开归档说明见 [本次发行记录](docs/PUBLICATION_20260922.md)。下文和根目录的 `PUBLICATION_CHECK.json`、`PUBLICATION_TRANSFORM.json` 保留为初次发行的历史记录。

本目录是为公开 GitHub 仓库制作的独立发行副本，项目采用 MIT License。原研究工作目录、私有发行副本及原始归档继续保存在本地，未被覆盖。

## 文件范围

保留算法源码、研究报告、已保存证书、原始评测结果的公开副本，以及旧版对照记录。旧版 release/runtime 中部分代码与结果仍被新测试或基线评估引用，因此保留整个目录结构。

排除了四份私有随机种子、系统及 Python 缓存、临时目录、下载的可选依赖和顶层过时发行 ZIP。账号授权文件位于仓库外，从未纳入发行包或 Git 历史。历史 tokenizer 数据保留原来源、哈希与第三方许可。

## 隐私处理与证据身份

历史本机路径已替换为中性前缀：`/PROJECT` 代表原项目根目录，`/PYTHON/bin/python3` 代表原 Python 可执行文件，`/LOCAL_USER` 代表原本地用户目录，`/LOCAL_TEMP` 代表操作系统临时目录前缀。它们是历史记录中的占位符，不能直接作为实际运行路径。请使用根 README 的命令。

所有 Python 源码保持原字节。部分报告、日志和 JSON 的路径文字改变了，因此其文件哈希也改变了。路径变化没有修改数学参数或计算结果。[PUBLICATION_TRANSFORM.json](PUBLICATION_TRANSFORM.json) 按相对路径记录原文件与公开文件的 SHA-256；这是一份变换记录，不是密码学意义上对历史事实的独立证明。

B001 的公开派生归档明确命名为 `source_snapshot.public.zip`。其中源代码不变；五份非源码数据和索引的本机路径已清理，索引中的字节数和哈希描述公开版成员。变换记录保留改变成员的原、新哈希。R03 的 `source.zip` 未修改。

历史 `freeze.json` 等文件中的原摘要继续表示原始文件；没有把它们悄悄改成公开派生文件的新摘要。原清单中的 `source_snapshot.zip` 指本地保存的原始归档，未随公开版发布。公开版不能声称所有历史文件仍通过原来的逐字节检查。当前发行核对见 [PUBLICATION_CHECK.json](PUBLICATION_CHECK.json)；私有副本在清理前的检查另存于 `docs/PRE_REDACTION_CHECK.json`，仅作为该阶段记录。

## 怎样复现

1. 保留目录结构，在仓库根目录使用 Python 3.12 运行 README 的示例及测试。旧说明中的占位路径应映射到本机环境。
2. 私有种子不随包发布；已有公开题目、承诺和结果仍可读取，证书可重放。已有题目不能重新计为未见题目。
3. 部分历史保全脚本绑定原机器或原文件哈希；它们不能直接作为公开派生包的完整复现检查。公开快照索引、源码哈希及当前接口已单独核对。
4. 本次发行检查不是正式数学评测重跑，也不是模型对照实验；原来的达标率、时间和 token 估算仍受各自报告的范围限制。
5. 当前最新入口使用 Python 标准库，不需要模型 API 密钥。历史可选实验按各自依赖说明运行。
