# B001 正式留出结果独立复核

结论：**B001 符合预先固定的正式验收口径。新方法 20/20 题各自 5/5 成功；冻结旧基线为 3/20 题各自 5/5 成功。** 门槛为至少 16/20，当前达到 100%，比该基线高 85 个百分点。配对增加 17 题，没有丢失原基线成功题。

这是一组已知数学族的新参数测试，不代表新数学结构泛化、重大开放问题解决或跨模型优势。5 次重复用于同一实例的稳定性与计时检查，数学实例仍只有 20 个。

## 独立核对范围与限制

只读 B001 的承诺、授权、冻结、揭示、抽样审计、归档、正式报告与 200 条原始运行记录；逐条重新计算身份、精确误差/谱宽、时限、稳定成功和分层汇总。没有读取新旧任何私有种子，没有修改被冻结资料或 Python 源码，也没有重新运行数学求解器。

完整数学证书在每个独立冷 worker 中由固定版本的验证器重放。本次复核对 140 条被该验证器接受的证书重新计算了精确上下界差或 L² 误差目标，并检查函数、空间、结论类型、容差与原始证据一致；它没有把同一验证器的数学公式再次实现或重新执行，因此不声称提供新的独立数学证明。其余 60 条运行没有有效证书，全部保留为失败。

## 题目与发布链

19 项发布前置核对均通过：

- 恰好 20 个不同数学身份，身份定义为规范化函数、结论种类和谱空间，忽略容差；与承诺目录中的 87 个已见身份没有重合。
- 固定布局保留 12 道负幂谱、6 道一般指数 L² 逼近、2 道阶跃谱。负幂谱包含 3 个精度 × 2 个空间 × 2 个强度；弱谱 6 题、强谱 6 题均未因结果筛掉。3 个精度的总题数分别为 6、8、6。
- 全部任务固定 z 轴、offset=0、在线预算 10 秒；一般指数逼近不使用固定 −1/4 算例。
- 根据公开抽样审计重新建立每个有序参数池、纯身份排除清单和已选身份集合，逐个核对抽样索引对应的题目，全部一致。没有重新读取私有随机数，因此没有独立再生随机字序列；种子的绑定依赖已保存的承诺与授权揭示链。
- root 的明确授权绑定 B001、协议、承诺和 freeze 哈希；reveal_started 绑定该授权与 freeze；revealed 绑定完整题目和抽样审计。全部哈希一致。
- 源码归档存在且本身哈希吻合。ZIP 索引包含 576 项，其中 562 份冻结 Python 源码全部具备实际字节；逐成员长度与 SHA-256 检查通过，协议、已见目录、回归指针及公开测试身份目录均已归档。

## 200 条运行记录

实际记录恰好覆盖两方法 × 20 题 × 5 个重复序号，共 200 个不同组合；没有缺项、重复或替换。

每条正式报告 row 都与对应磁盘 `*_result.json` 完全一致。200 份 `*_spec.json` 的原始任务、方法、重复序号与题目登记一致，且全为正式 worker 而非合成测试。200 份 output、200 份 checkpoint、200 份 log 均存在，报告中的 raw_paths 均位于本批次 raw 目录；原始 output 的证书、判断、任务、计时阶段与汇总 row 相符。

重新计算的成功条件同时要求：有效证据、精确误差达标、原任务绑定正确、完整执行成功、父看门狗没有触发、无协议错误、工作进程正常退出，并且单调时钟的开始/结束差值不超过 10 秒。各事件时间差与已记录在线时间完全一致。没有仅依据 solver 的 status/verified/target_met 标志计成功。

| 指标 | 新方法 | 冻结旧基线 |
|---|---:|---:|
| 原始运行数 | 100 | 100 |
| 成功运行 | 100 | 15 |
| 每题 5/5 稳定成功 | 20/20 | 3/20 |
| 有效数学证书 | 100 | 40 |
| 负幂谱稳定成功 | 12/12 | 3/12 |
| 一般指数 L² 逼近稳定成功 | 6/6 | 0/6 |
| 阶跃谱稳定成功 | 2/2 | 0/2 |
| 1e−6 稳定成功 | 6/6 | 1/6 |
| 1e−8 稳定成功 | 8/8 | 1/8 |
| 1e−10 稳定成功 | 6/6 | 1/6 |

旧基线成功身份为 N03、N07、N11。85 次失败包括 25 次证书有效但精度不足、30 次未返回证书、30 次一般指数逼近入口明确拒绝其不支持的指数。没有超时导致的失败；所有失败仍属于固定分母。报告原始 failure_reason 对前 25 次写作 `independently_replayed`，应解释为“已重放但未达精度”，不能误读为成功。

重新计算的总数、每题 5 次结果、族/空间/强度/精度分层、配对增减和正式 >=80% 条件全部与 formal_report.json 相符。未发现差异。

## 计时统计

以下时间来自父进程和 worker 的原始计时，未删掉失败。单题在线范围包含求解、首次使用的延迟导入、完整 JSON 编解码、独立证书重放和首份完整 raw 写盘；冷进程时间另含启动、初始化和退出。

| 统计，单位秒 | 新方法 | 冻结旧基线 |
|---|---:|---:|
| 单次在线 wall 中位数 | 0.429888 | 0.007284 |
| 单次在线 wall 最大值 | 2.208015 | 0.450057 |
| 单次冷 wall 最小值 | 0.041951 | 0.086099 |
| 单次冷 wall 中位数 | 0.464666 | 0.090470 |
| 单次冷 wall 最大值 | 2.245271 | 0.534317 |
| 100 次冷 wall 合计 | 63.793142 | 16.695758 |
| 100 次冷 CPU 合计 | 63.529118 | 16.455911 |
| 每轮 20 题冷 wall 中位数 | 12.747726 | 3.339118 |
| 每轮 20 题冷 wall 最小–最大 | 12.719361–12.793588 | 3.322277–3.350577 |
| 每轮 20 题冷 CPU 中位数 | 12.690798 | 3.292312 |

五轮新方法冷 wall 总时间分别为 12.719361、12.747726、12.742950、12.789517、12.793588 秒；旧基线为 3.322277、3.346272、3.339118、3.337514、3.350577 秒。整个正式评估的记录 wall 时间为 83.368842 秒，包含两方法与评估器开销。

新方法最慢在线运行 2.208015 秒，距 10 秒上限尚有 7.791985 秒；100 次均无看门狗终止。旧基线多数失败迅速返回，因此较短时间不能解释为同等完成度下的速度优势，也不能把新方法的完成率提升表述成提速。共享主机时间仅作描述性统计。实际模型输入、输出、推理 token 与费用仍未知。

## 冻结与历史保全

复核时实际磁盘上的全部 562 份冻结源码及所有冻结 artifact 哈希均与 freeze.json 一致；正式报告也正确绑定 freeze 与已揭示题目的哈希。

另外实际执行了既有只读历史完整性审计，并与冻结记录逐字段比较：runtime manifest 18 项通过，旧文件保全 914 项通过，旧数学基线 1139 项通过；无 changed_or_missing，无冻结旧目录新增文件。此步骤只检查文件与既有冻结关系，没有数学求解，也未读取私有种子。

## 主要证据文件

- 正式报告：`evaluation/batches/B001/formal_report.json`。
- 题目与新颖性目录：`holdout_cases.json`、`known_catalog.json`、`sampling_audit.json`。
- 发布链：`commitment.json`、`authorization.json`、`freeze.json`、`reveal_started.json`、`revealed.json`。
- 保全：`source_snapshot.zip`、`historical_preservation.json`，以及 freeze 绑定的两份最终五重复回归报告。
- 全部原始证据：`evaluation/batches/B001/raw/` 下 200 组 spec/output/checkpoint/log/result 文件；每份正式 row 的 raw_paths 给出对应引用。

本复核只确认这次已冻结、已授权且全量保留的 B001 达到预先固定标准。若其参数或结果用于后续优化，B001 应继续作为公开开发/回归资料，不能再次包装成独立新批次。
