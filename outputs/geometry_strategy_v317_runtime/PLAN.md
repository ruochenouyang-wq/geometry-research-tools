# 冷启动退步修订

上一修订的14题评测保持不变：同为5/14，新服务相对冻结v317完整墙钟和CPU减少约37%，但旧6题全员6/6时新服务冷成本仍比v317多5.9%。按照goal中“修复退步”的要求，不能仅凭前一个门槛宣布完成。

本目录只修复初始化：奇异数学私有模块按需构造，普通请求仍经完整冻结验证；证据存储和交互组件在首次使用时初始化并计费；完整源码版本标识每服务实例只计算一次。数学候选、原题、空间、容差、首次证据核验和独立重放都保留。首次存储使用前重新核对路径，不能利用延迟初始化改变路径绑定。

这些是80轮后的真实验收修复，不额外标成第81、82轮。原80轮与上一修订均只读保存；所有失败计时和不达标题仍保留。评估员在查看新种子前写死比较规则。新留出和已有任务的重合项保留并标明，不能把重复任务称为未用实例。

本轮仍是本地工具研发；真实跨模型挑战、模型理解率、完整历史分项账单不能由本地计时替代。所有新增计算与审查证据保存，能取得的总体goal计数和分项实验计数分别报告。
