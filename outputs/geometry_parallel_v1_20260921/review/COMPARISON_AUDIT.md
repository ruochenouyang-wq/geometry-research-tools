# 最终比较结果的独立只读复核

复核对象：`evaluation/final_20260922T013006751900Z/report.json`，冻结记录为 `evaluation/freeze_20260922T012957425228Z.json`。本次只读取本轮目录内的公开协议、题单、逐题输出、比较源码、审查说明及冻结 ZIP，没有读取其他轮留出题或任何种子，没有运行求解器、性能测试或大型数学重放，也没有修改已冻结文件。仅新增本审查文档。

**结论：汇总的关键数字与逐题记录一致，未发现阻断最终公开开发比较的错误。** 结论严格限定于这次已知题比较和已记录的验证结果；本审查不替代三个数学实现的独立审查，更不构成未见题、跨模型或形式化数学证明。

## 1. 原始记录核对范围

- 64 条共同任务记录：8 题 × 4 系统 × 2 次，逐项核对 `result.json`、`output.json`、`spec.json` 与汇总中的完整响应和判断；核对原函数、空间、原精度及精确分数指标。
- 24 条扩展任务记录：6 题 × 4 系统；有效计数与负谱和精度分开核对，所有不支持和精度不足保留。
- 39 条攻击记录：逐项核对原始验证器的 `certificate_valid:false`、期限内完成及汇总状态，不以求解器状态或超时冒充拒绝。
- 8 个参数会话、128 个原始查询与128个独立冷重放：逐项核对查询原题和完整证书等于冷重放输入，核对准备文件、阶段事件、累计时间与冷重放成本。

合计检查 255 组 `result.json/output.json`（共同题、扩展题、攻击、冷重放），另核对 8 份会话结果、准备记录与128份查询文件。所有查询及冷重放均成功，没有从统计中剔除失败分支或缺失行。

## 2. 共同题与 C 的同成功成本

四个系统均为 8/8 题两次都成功，即每系统 16/16 次运行达标。谱指标重新按 `upper−lower`、逼近指标按 `error_upper` 用精确分数与原容差比较；所有值非负且不超过 `1e−8`。A/B 的普通任务回退明确标注，不能记为新增能力。

| 系统 | 完整冷进程总 wall / 秒 | 总 CPU / 秒 |
|---|---:|---:|
| baseline | 8.368873914 | 8.309521 |
| A | 8.553637749 | 8.502316 |
| B | 8.314607210 | 8.261170 |
| C | 5.529927415 | 5.478571 |

C 相对 baseline 的总 wall 降幅为 **33.9227%**，CPU 降幅为 **34.0688%**。逐题中位数比值也重新计算并与机器报告相符。D03、D04、D06 的 wall 分别减少约 36.10%、45.30%、53.57%；其他题基本沿用原路线，没有共同成功数量增益。

D04 的原始宽度确为 baseline `6.1440086104e−13`、C `9.3730632634e−9`。因此结论是满足同一目标的成本下降，不能描述为精度提高。只有两次重复，仍是初步速度证据；不支持普遍或统计显著的加速结论。

## 3. B 的准备、前缀与独立冷重放计费

两个 B 会话的准备阶段分别约为 wall `0.733439 / 0.732500` 秒、CPU `0.732953 / 0.732039` 秒；初始化另约 `0.035920 / 0.035965` 秒。阶段源码及事件共同确认：准备包含建库、完整 family 验证和 `prepare_context` 的首次验证，查询使用已准备上下文；其完整证书随后又在独立新进程中重放。两份银行的原始统一宽度相同，约为 `7.68278661217e−7`，银行验证结果确为达标。

下表从原始事件与逐题冷进程成本重新累加，统计量为两会话中位数，单位秒，均已加入额外完整冷重放：

| N | baseline wall / CPU | B wall / CPU | C wall / CPU |
|---:|---:|---:|---:|
| 1 | 0.476998 / 0.472643 | 0.930211 / 0.926009 | 0.340907 / 0.336787 |
| 4 | 1.629173 / 1.616968 | 1.419465 / 1.408273 | 1.075361 / 1.064367 |
| 16 | 6.284483 / 6.239053 | 3.368033 / 3.325853 | 4.024850 / 3.984647 |

这些是**从会话启动到第 N 题完成的累计前缀**，已经包含准备、输出落盘与要求的验证，但不是分别启动 N 题短会话再退出的总成本。最终退出及 B 的额外盒外检查尾部仍在 `process` 总成本中，不能说其不存在。完整16题会话退出后再加全部冷重放的中位 wall / CPU 是：baseline `6.290692 / 6.244082`，B `3.373792 / 3.330601`，C `4.030299 / 3.989195` 秒。这一区别约5–6毫秒，不改变路线选择。

按报告的前缀口径，B 在 N=16 相对 baseline 不加额外冷重放时减少80.6334%，加后减少46.4072%；相对 C 加冷重放后只减少16.3190%，未达20%阈值。N=1 的 B 明显较慢，N=4 已低于 baseline，但仍高于 C。只能定位已测检查点，不能声称已经找出精确盈亏平衡点。

四系统各32次查询及32次独立冷重放全部成功。B 两次盒外查询均由明确 `ValueError` 拒绝，均非超时或不确定拒绝。

## 4. A 的计数与负谱和

| 题号 | 已认证严格负谱个数 | 精确分数换算的和区间宽度 | 和目标1e−6 |
|---|---:|---:|---|
| E01 | 4 | 5.9604645580e−8 | 达标 |
| E02 | 3 | 4.4703484248e−8 | 达标 |
| E03 | 9 | 1.0058283828e−7 | 达标 |
| E04 | 8 | 8.5681677198e−8 | 达标 |
| E05 | 4 | 5.3061631124e−5 | 未达标 |
| E06 | 8 | 2.8989749572e−5 | 未达标 |

计数6/6、和精度4/6、两者同时成功4/6均与原始记录一致。E05/E06 完整进程分别约6.457607和7.363978秒；均未超10秒，但和精度不足仍是失败。其余系统的这6题全部明确不支持，不能合并入共同8题的正确率或据此宣称 A 普遍胜出。

前四题可解析校准；E05 的点态夹逼给出独立计数，E06 的独立序号界推导已在 `review/A_REVIEW.md` 记录。这些独立计数依据不提供 E05/E06 的精确负谱和，报告对此区分正确。

## 5. 攻击覆盖的准确边界

最终39次攻击全部期限内得到明确拒绝，错误接受0。实际分组为：四系统各4次 spectrum、各3次 approximation，A negative_spectrum 5次，B family_query 6次；与预声明10个“系统×任务类型”组完全一致。B 两次盒外拒绝另计。

代表采样并非每个证书格式和困难分支的穷尽覆盖：共同 spectrum 代表都是 D01 的 `step_c1_strong_residual_temple_v1`，所以 C 此组攻击测试的是回退证书；A 的负谱代表是 E01 常势；B 则另有原生 `amplitude_family_point_query_v1` 的银行宽度与覆盖攻击。开发阶段的各路线独立数学攻击应单独引用，不能混入最终39次或把它们说成同一次冻结后对照。

## 6. 冻结与567文件保全

本次直接重算冻结清单文件和 ZIP 的哈希，并逐个解包重算 ZIP 索引中的 **584 项**：572个源码、12个协议/资料文件，全部匹配冻结身份；最终报告的 `identity_before`、`identity_after` 与冻结身份相等。另直接复核本轮目录内22个冻结文件的当前哈希，均未变动。

`BASELINE.json` 声明567个旧 Python 文件。其中562份原文在本次 ZIP 中，其字节哈希已与原声明逐一匹配；另外5份 `outputs/math_research/{stability_research.py,run.py,math_research.py,test_math_research.py,test_stability_research.py}` 没有进入这个只扫描 `geometry_*` 的源码 ZIP，但其预先记录的哈希仍由已冻结 `BASELINE.json` 绑定。机器结果记录运行后567/567一致；根任务另确认重新执行 `checked_freeze()` 与 `baseline_preserved()` 仍通过。本审查遵守仅本轮材料限制，没有再次打开这5份外目录源码，因此不把该部分称为本审查独立逐文件重读。

这不改变本次比较数字；但不能写“567份旧源码原文全部已打包”，也不能把本轮 ZIP 当作无需相邻历史依赖的完整分发包。现有 README 要求保留相邻历史模块，与该边界一致。

## 7. 中文报告复核

已对照 `REPORT.md` 和 `README.md` 中的成功数、成本、比例、精度差异及 A 的两项失败，关键数字相符。已反馈并由根任务处理的文字边界包括：前缀计费与完整退出成本分开、残差是代入特征方程的偏差而非无条件的特征函数距离、代表攻击不能等于原生所有分支覆盖、ZIP与567项哈希保全范围分开。

应继续保留公开开发、两次重复、已知失败不删除、没有真实模型token观测、没有跨模型或开放问题突破结论等限制。最终结论支持对 C 的普通任务加速、B 的固定族批量复用、A 的负谱计数分别继续验证，不支持把三种不同扩展压成一个统一正确率。
