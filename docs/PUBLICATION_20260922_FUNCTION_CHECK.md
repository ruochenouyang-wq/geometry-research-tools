# 公开副本功能检查

独立检查全部通过，记录见 [PUBLICATION_20260922_CHECK.json](PUBLICATION_20260922_CHECK.json)。

- C3.1 `GuardControls` 实际运行 14 项，全部通过。
- `examples/c31_e18.jsonl` 经公开副本的 CLI 入口执行一次，随后用公开副本中的原 `baseline.assess` 完整重放。证书有效且达到原 `1e-6` 容差，精确区间宽度约为 `7.242073270893462e-7`。
- 公开 E19 仅执行一次，覆盖可选 NumPy 2.3.5 快候选路径及原验证器重放。首次快候选不满足筛选条件时保留有理后备，后续快候选达到精度；最终有效区间宽度约为 `5.2811462560117824e-11`，达到原 `1e-8` 容差。

两个数学例子的外层整个进程组均设 10 秒截止，包含入口加载、求解、JSON 和额外原验证器重放；均正常完成。执行期间记录瞬态代码执行与模块导入来源，并核对保留模块及作用域模块；观察到的项目 Python 全部位于公开副本中，没有加载原研究工作目录代码。详细记录只使用仓库相对路径。

本检查未修改数学源码或公开题目，未上传仓库，也未重跑 180/48 次基准。计时仅用于确认功能与期限，当时另有归档压缩，不能解释为正式性能结果。模块来源结论限于本次实际执行分支；完整复制一致性及发布转换由独立的 [完整性检查](PUBLICATION_20260922_INTEGRITY.json) 覆盖。
