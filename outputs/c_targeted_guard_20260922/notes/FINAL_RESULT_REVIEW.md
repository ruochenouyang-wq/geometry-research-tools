# C3G 补充结果独立审查

结论：无阻断。此次为观察原 C3 的 N29 精度退步后，预先固定八道已公开原题的定向回归；不得替代、并入或冒充原先 180 次比较，也不能据此推断全部 30 题的 C3G 成绩。

审查对象为 `evaluation/final_20260922T203916998063Z/report.json`。只读解析原始 `spec.json`、`result.json`、`output.json`、`response.json` 与证书，完成 2,161 项记录一致性断言；没有重新运行数学求解或性能测试。48 个唯一组合恰为八题 × C3/C3G × 三次，失败未从分母移除。各行与原任务、任务摘要和落盘记录一致；36 份证书的函数、空间、容差及精确指标与原题绑定，记录的旧完整验证器均接受，成功标志与精确容差判定一致。此检查复核已有完整重放记录，并非另行数值重放或形式化证明。

两版本均为七道数学题中五题三次稳定成功，即各 15/21 次数学成功。E24 是单列的接口边界控制，各 3/3 次正确拒绝，不计作数学成功。全 48 行为 30 成功、6 有效但精度不足、6 无证书、6 不支持；没有丢失旧成功或把精度不足误记成功。

N29 在 C3G 的三次运行中均只尝试一次旧基线，并由父进程原验证器接受后严格缩窄，精确宽度均为：

`551139446120889852335259/1083197534374707740536733696`

这与原 C2 的宽度完全相等，约为 `5.088078846477835e-4`，相对原 C3 的 `1.392945120676351` 恢复了精度；但仍大于原容差 `1e-6`，因此三次均保持 open，成功数没有增加。外部恢复阈值未进入求解任务。N29 每次均记录一次守护旧基线调用、一次父重放及正的子进程 CPU，未漏计恢复代价。

其余七题均未触发守护后备，逐重复证书内容、路由与原成功状态相同。E18、E13、E08、E19、E21 保持成功；N30 仍无证书；E24 仍拒绝。五道共同成功题每版本各 15 次，其原生 C3 成功为 6 次（E18/E19），C1 复用 3 次、C2 复用 3 次、旧基线后备 3 次；守护改善 N29 不应算成原生成功。

| 计量范围 | C3 | C3G |
|---|---:|---:|
| 全 24 次完整进程 wall 总计（秒） | 44.345797 | 53.840232 |
| 全 24 次完整进程 CPU 总计（秒） | 43.928233 | 53.305006 |
| N29 单次 wall 中位数（秒） | 3.753942 | 7.071675 |
| N29 单次 CPU 中位数（秒） | 3.733464 | 7.026913 |
| N29 RSS 中位数（MiB） | 37.031250 | 37.484375 |
| 五题同成功 15 次 wall 总计（秒） | 31.967004 | 31.657331 |
| 五题同成功 15 次 CPU 总计（秒） | 31.694068 | 31.352229 |
| 五题同成功证书总字节 | 509142 | 509142 |
| 全 24 次响应总字节 | 812425 | 953697 |

全部进程正常退出，无硬超时、协议解析错误或强制结束造成的 CPU 覆盖缺口；最大完整 wall 为 7.196450 秒，低于同一 10 秒上限。CPU 已包含回收的后备子进程，不再次累加；RSS 是最大可观察单进程高水位，并非同时运行的进程树总和。C3G 保存后备轨迹增加了响应体积，JSON 字节不等于内存或 token 数。三个重复与共享机器上的小幅旁路耗时变化不支持统计显著性或包装器加速结论。

独立身份复核确认：644 份 ZIP payload 与当前文件逐字节及 SHA-256 一致，归档恰有 645 成员（含 index）；原 C3 的 636 文件全部保留，新增六份源码/协议/题单文件及两份冻结引用一致。八题与原 C3 的完整记录相同；报告运行前、运行后及冻结身份相同，环境与冻结记录完全相同。线程环境要求均为 1，但未将请求的线程限制冒充实测线程数量。身份复核由独立审查代理执行，只读且未访问种子。

关键摘要：

- 报告：`0534de9f1ea0a2987199dd20647161e283033f0fef531f2727c7d115b241d20a`
- 冻结清单：`b0bdc867100a403dd5eb78f35a952cc850f18d37e001c283c110a05e167d0f32`
- 归档：`559849d08eb161d4e877c457d9cc36f19b73722c78b375ac08ada0d4ed915b2f`
- 守护控制器：`65ed64f30a2af2d0884a6123ad881d714d22f7e65ba7cf258e77d01d72882a36`

修订的取舍是以额外完整后备与验证成本，修复一个已知 open 区间的精度退步；它没有在本次八题回归中新增达到原容差的任务。
